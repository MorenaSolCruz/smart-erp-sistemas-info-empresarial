from django.apps import AppConfig


class SuppliersConfig(AppConfig):
    # Registra el modulo de proveedores dentro de Django.
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.suppliers"

