from django.apps import AppConfig


class StatisticsConfig(AppConfig):
    # Registra el modulo de estadisticas dentro de Django.
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.statistics"

