from django.apps import AppConfig


class WasteConfig(AppConfig):
    # Registra el modulo de desechos/mermas dentro de Django.
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.waste"

